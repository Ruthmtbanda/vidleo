name: vidleo
description: VIDLEO - an offline-first short video creator app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^8.0.6
  path_provider: ^2.1.4
  path: ^1.9.0
  video_player: ^2.9.1
  permission_handler: ^11.3.1
  process_run: ^0.14.2
  uuid: ^4.5.1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
